#include <stdio.h>
#include <locale.h>

 main() {

  setlocale(LC_ALL, "portuguese");

   int valor = 1;

   while (valor < 51){

     int par = valor * 2;

     printf("%d\n", par);

     valor++;
     
   }
   
     
}
